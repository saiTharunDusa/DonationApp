import React from 'react';
import {
  SafeAreaView,
  Text,
} from 'react-native';

const App = () =>  {
  return(
    <SafeAreaView>
      <Text style={{
        fontSize:70,
        fontFamily : 'Inter'
      }}>Hello World!</Text>
    </SafeAreaView>
  )
};

export default App;
